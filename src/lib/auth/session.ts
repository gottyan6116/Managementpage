import { createHmac, randomBytes, timingSafeEqual } from "node:crypto";
const SESSION_DURATION_MS = 12 * 60 * 60 * 1000;
function signature(value: string, secret: string): string { return createHmac("sha256", secret).update(value).digest("base64url"); }
export function createSessionToken(secret: string, now = new Date()): string { const payload = `v1.${now.getTime() + SESSION_DURATION_MS}.${randomBytes(16).toString("base64url")}`; return `${payload}.${signature(payload, secret)}`; }
export function verifySessionToken(token: string | undefined, secret: string | undefined, now = new Date()): boolean { if (!token || !secret) return false; const parts = token.split("."); if (parts.length !== 4 || parts[0] !== "v1") return false; const [version, expiryValue, nonce, providedSignature] = parts; const expiresAt = Number(expiryValue); if (!Number.isSafeInteger(expiresAt) || expiresAt <= now.getTime() || !nonce || !providedSignature) return false; const expected = Buffer.from(signature(`${version}.${expiryValue}.${nonce}`, secret), "base64url"); const actual = Buffer.from(providedSignature, "base64url"); return actual.length === expected.length && timingSafeEqual(actual, expected); }
